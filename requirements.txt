# Orpheus-FastAPI Requirements
# Core dependencies for the FastAPI TTS server

# FastAPI framework
fastapi>=0.100.0
uvicorn>=0.22.0

# Environment and configuration
python-dotenv>=1.0.0

# HTTP client for LMStudio API
requests>=2.28.0

# Audio processing
numpy>=1.24.0
scipy>=1.10.0

# PyTorch (CPU version - install GPU version separately if needed)
# For CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.0.0

# SNAC audio codec for Orpheus
snac>=1.0.0

# Pydantic for data validation
pydantic>=2.0.0

# Optional: for audio playback (development)
sounddevice>=0.4.6
